# 이 저장소에서 일하는 법

## 커밋 메시지

```
type(scope): 무엇을 했는지 한 줄

왜 문제였는지, 무엇을 바꿨는지. 본문은 6줄 안쪽.
```

`type` 은 feat · fix · docs · chore, `scope` 는 web · api · ai · db ·
contracts · design · deploy · build · dev 중 하나. 스코프는 빼지 않는다.

**제목은 명사형으로 끝낸다.** "~한다" 로 끝내지 않는다.

```
매칭한다   ->  매칭
깨워둔다   ->  예열
내린다     ->  내림
붙인다     ->  추가
```

**본문에 쓰지 않는 것**

- 어떻게 찾았는지 ("로컬에서 띄워 보고 찾았다", "배포된 서버에서 재현했다")
- 어떻게 확인했는지 ("실제로 꺼서 세 경우를 재현해 확인했다")
- 제품 철학 — 코드 주석이나 README 자리다
- ASCII 표와 before/after 도식

작업 일지가 아니라 나중에 `git log` 를 읽을 사람을 위한 글이다.
바꾼 이유와 결정만 남긴다.

**트레일러를 붙이지 않는다.** `Co-Authored-By` 도, 도구 생성 표기도 넣지 않는다.

## 화면을 손대기 전에

`docs/design-reference/` 에 설계 원본이 화면별로 쪼개져 있다.
기억이나 추측이 아니라 파일을 열어 대조한다.

만들기 전에 그 화면이 개정 설계에 아직 있는지부터 확인한다.
22종에서 26종으로 개정되면서 빠진 화면이 있다.

## 고쳤으면 띄워서 본다

타입체크와 빌드가 통과해도 화면은 틀릴 수 있다. 실제로 실행해 눈으로 본다.
이 저장소에서 그렇게 잡은 버그가 여럿이다 — 헤드라인 숫자가 카드 수와
달랐던 것, 쉬어가기가 제자리에 남던 것, 설계에 없는 줄이 남아 있던 것.
